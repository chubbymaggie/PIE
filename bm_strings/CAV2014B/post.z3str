(declare-const r String)
(declare-const i Int)

;; Invariant :: (= i (str.len r))

(define-fun post_check () Bool
            (=> (and (= i (Length r)) (not (>= (Indexof r "aa") 0)))
                (and (= i (Length r)) (not (Contains r "aa")))))

(assert (not post_check))
(check-sat)
